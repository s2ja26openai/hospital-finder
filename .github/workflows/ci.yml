name: CI

on:
  push:
    branches: [main, sprint3]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements-dev.txt

      - name: Run tests with coverage
        env:
          KAKAO_JS_API_KEY: ""
          KAKAO_MAP_API_KEY: ""
          ANTHROPIC_API_KEY: ""
        run: pytest --tb=short -v --cov=services --cov=routers --cov-report=term-missing

      - name: Check coverage threshold
        env:
          KAKAO_JS_API_KEY: ""
          KAKAO_MAP_API_KEY: ""
          ANTHROPIC_API_KEY: ""
        run: pytest --cov=services --cov=routers --cov-fail-under=40 -q
